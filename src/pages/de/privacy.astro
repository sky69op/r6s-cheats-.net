---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getTranslations, defaultLocale } from '../../i18n';
import { pageTitle } from '../../config/seo';
import { localizedPath } from '../../utils/path';
import LegalPage from '../../components/LegalPage.astro';
import { breadcrumbSchema } from '../../utils/schema';

const locale = Astro.currentLocale ?? defaultLocale;
const t = getTranslations(locale);
const content = t.legalPages.privacy;
---

<BaseLayout
  title={pageTitle(content.title)}
  description={content.description}
  jsonLd={[
    breadcrumbSchema([
      { name: t.ui.breadcrumbs.home, path: localizedPath(locale, '/') },
      { name: t.footer.privacy, path: localizedPath(locale, '/privacy/') },
    ]),
  ]}
>
  <LegalPage
    title={content.title}
    description={content.description}
    breadcrumbLabel={t.footer.privacy}
    sections={content.sections}
  />
</BaseLayout>
